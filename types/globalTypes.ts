interface User {
  id: string;
  name: string;
  email: string;
  password: string;
}

export default User;

export interface UserList {
  avatar: string;
email: string;
first_name: string;
id: number;
last_name: string;
}